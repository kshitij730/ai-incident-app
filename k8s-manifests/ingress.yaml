apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aiops-ingress
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: aiops-incident-service
                port:
                  number: 80
